[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[project]
name = "racingedge-model"
version = "0.1.0"
description = "RacingEdge Phase 2 probability model — feature engineering, training, calibration, backtesting."
requires-python = ">=3.11"
