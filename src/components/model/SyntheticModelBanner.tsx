export function SyntheticModelBanner() {
  return (
    <div className="rounded-md border border-warning/40 bg-warning-soft px-3 py-2 text-sm font-medium text-warning">
      SYNTHETIC TEST MODEL — NOT FOR BETTING USE. Trained wholly or partly on synthetic sample data; predictions
      below demonstrate the pipeline only and carry no real-world edge.
    </div>
  );
}
